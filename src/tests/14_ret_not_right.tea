struct MyStruct { 
    node:int,
    len:int  
}

let a:MyStruct;
let b:int;

fn main()->int{    
    ret b;
}

fn man()->int{    
    ret;
}

fn my()->int{    
    ret a;
}

// 有返回类型，测试返回不同的类型/未赋值的类型 yes
// 返回空 xx
