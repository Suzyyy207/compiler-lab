let n:int;
let m:int;
let to[5005]:int;
let next[5005]:int;
let head[1005]:int;
let cnt:int = 0;
let que[1005]:int;
let h:int;
let tail:int;
let inq[1005]:int;

fn addedge(from:int, To:int){
	to[cnt] = To;
	next[cnt] = head[from];
	head[from] = cnt;
	cnt = cnt + 1;
	to[cnt] = from;
	next[cnt] = head[To];
	head[To] = cnt;
	cnt = cnt + 1;
}