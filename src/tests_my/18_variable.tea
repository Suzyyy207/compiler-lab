fn foo(a:int)->int{
	if (a > 0){
		let i = 0;
		while (i < a){
		}
	}

	if (a < 0){
		let i = 0;
		while (i > a){
		}
	}
}

struct MyStruct { 
    node:int,
    len:int  
}

let a : MyStruct;
fn foo1(a : int)->int{
	ret 2 * a;
}

let x:int = a + 5;
let a:int = 5;